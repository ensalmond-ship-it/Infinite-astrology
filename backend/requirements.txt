# Backend requirements for Infinite Astrology Hub
# These are the tools Python needs to run the server

Flask==2.3.0
Flask-CORS==4.0.0
python-dotenv==1.0.0
requests==2.31.0

# Future: Astrology calculations
# ephem==4.1.5          # For astronomical positions
# pytz==2023.3          # For timezone handling